{{- if .Values.ontopic.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: ontopic-store-server-db
spec:
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  selector:
    app: ontopic-store-server-db
  type: ClusterIP
{{- end }}
